距离的度量对于很多基于距离的机器学习方法而言是十分重要的，例如分类算法k近邻，聚类算法k-means等。对于数据集中的样本点，使用合理的距离函数去度量它们之间的距离是这些算法的关键。本题关注曼哈顿距离（Manhattan Distance）的理解。

曼哈顿距离，也被称为出租车几何，是一种用来度量多维实数空间内两个样本点之间距离的方法。假设多维空间的维度为N，x1=x11,x12,⋯,x1N和x2=x21,x22,⋯,x2N是两个样本点，则x1和x2之间的曼哈顿距离计算如公式(1.2)：
d(x1,x2)=∑n=1N|x1n−x2n|(1.2)
曼哈顿距离等于两个样本之间每一维度之差的绝对值之和。曼哈顿距离的含义可以对应到规划为方框建筑的城市（如曼哈顿），两个地点的出租车最短行驶距离。在使用曼哈顿距离时，也需要考虑变量之间取值范围不同对结果的影响。

预设变量
本题使用的数据变量名、含义及其类型如下：

变量名	含义	类型
x1	数据样本1	ndarray
x2	数据样本2	ndarray
答题要求
根据题目要求，给出计算结果manhattan_dis，用于后台对本题正误进行判断。结果保留两位小数。

manhattan_dis = None
import numpy as np
manhattan_dis = np.sum(np.abs(x1 - x2))
